services:
  postgres:
    image: postgres:18
    container_name: movie-library-db
    environment:
      POSTGRES_DB: movie_library
      POSTGRES_USER: movie_user
      POSTGRES_PASSWORD: movie_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: